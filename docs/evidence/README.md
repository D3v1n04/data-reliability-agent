# Public evidence

Only approved, redacted, synthetic-data screenshots belong in this directory.
Use the filenames and review procedure in `docs/demo-guide.md`.

Raw AWS console, CockroachDB console, terminal, and log captures must remain
outside the repository until account IDs, emails, hosts, credentials, and
unrelated operational data have been removed.
